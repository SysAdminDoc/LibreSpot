{
  "$comment": "Keyboard and focus contract for the WPF desktop shell. Defines the expected tab order, default/cancel buttons, Escape behavior, focus trap/restoration, and screen-reader naming for every interactive surface.",
  "schemaVersion": 1,
  "globalBindings": [
    {
      "key": "Escape",
      "command": "EscapeCommand",
      "behavior": "Dismisses the topmost overlay (prompt, then activity) or navigates back if no overlay is open."
    },
    {
      "key": "F5",
      "command": "RefreshSnapshotCommand",
      "behavior": "Refreshes the environment snapshot from disk."
    }
  ],
  "overlays": [
    {
      "name": "PromptOverlay",
      "tabNavigation": "Cycle",
      "focusOnShow": "PromptConfirmButton (normal) or PromptCancelButton (destructive action)",
      "focusOnDismiss": "Restores focus to the element that was focused before the prompt opened.",
      "defaultButton": "PromptConfirmButton (data-bound via IsPromptConfirmDefault)",
      "cancelButton": "PromptCancelButton (IsCancel=True — Escape dismisses)",
      "contract": [
        "Tab cycles within the prompt; does not leak to main content.",
        "Destructive prompts focus Cancel first to prevent accidental Enter.",
        "Escape closes the prompt without executing the action.",
        "Focus is restored to the prior element when the prompt closes."
      ]
    },
    {
      "name": "ActivityOverlay",
      "tabNavigation": "Cycle",
      "focusOnShow": "ActivityDialogRoot (focusable Border, receives initial focus)",
      "focusOnDismiss": "Restores focus to the element that was focused before the activity opened.",
      "contract": [
        "Tab cycles within the activity overlay; does not leak to main content.",
        "While running: root element is focused so screen readers can announce progress.",
        "After completion: focuses the first available action button (close, copy log, open folder).",
        "Focus is restored to the prior element when the activity closes."
      ]
    }
  ],
  "pages": [
    {
      "name": "Recommended",
      "railLabel": "Home",
      "initialFocus": "RunRecommendedSetupButton (primary action, centre of the page)",
      "tabOrder": "Rail buttons (WorkspaceNavRecommended → WorkspaceNavMaintenance → WorkspaceNavCustom) → SimpleShellLanguageSelector → workspace content → RunRecommendedSetupButton → RecommendedDetailsExpander",
      "notes": "Single-action page. The rail entries are Buttons, not RadioButtons, and each carries an AutomationProperties.Name matching its localized label. In the snapshot-error state RetrySystemCheckButton replaces the duration line and is the QA focus target."
    },
    {
      "name": "Custom",
      "railLabel": "Settings",
      "initialFocus": "SettingsSearchBox, then the first option in workspace content",
      "tabOrder": "Rail buttons → SimpleShellLanguageSelector → SettingsSearchBox → option groups → theme/scheme selectors → extensions → ApplyCustomProfileButton",
      "notes": "Settings search box is reachable via Tab and SettingsSearchClearButton appears once it has text. Search results are focusable."
    },
    {
      "name": "Maintenance",
      "railLabel": "Maintenance",
      "initialFocus": "First health card or action button",
      "tabOrder": "Rail buttons → SimpleShellLanguageSelector → compatibility verdict → health cards → action buttons",
      "notes": "Destructive actions (Full reset, whose button reads Reset everything) use warning/danger styling as non-color-only indicators."
    }
  ],
  "overlaySurfaces": {
    "$comment": "WCAG 2.2 SC 2.4.11 (Focus Not Obscured) asks that a focused control is not entirely hidden by author-created content. Axe.Windows 2.4.2 has no rule for it. These are the only surfaces in the shell that may paint over content, so WpfUiAutomationSmokeTests checks focusable elements against exactly these; anything else that starts covering content is a defect, not an entry to add here.",
    "mayOverlayContent": [
      {
        "automationId": "PromptDialogRoot",
        "reason": "Modal prompt. Tab cycles inside it and cannot reach the content underneath, so covering that content is correct."
      },
      {
        "automationId": "ActivityDialogRoot",
        "reason": "Modal activity dialog. Same containment: focus stays inside while it is open."
      }
    ],
    "notAnOverlay": [
      {
        "name": "Settings apply bar",
        "reason": "The Apply custom profile bar looks sticky but is Grid.Row=1 of a two-row grid whose first row holds the scroll viewer, so it occupies its own space and cannot cover scrolling content. Recorded because a reading of the Settings screenshot suggested otherwise."
      }
    ]
  },
  "focusVisuals": {
    "strategy": "Custom ring-based indicators replace system FocusVisualStyle on all interactive controls.",
    "controls": [
      "Button: 2px AccentRingBrush overlay ring on IsKeyboardFocused, Margin=-3 (no layout shift)",
      "ComboBox: Same overlay ring pattern",
      "CheckBox: Same overlay ring pattern with Opacity animation",
      "TabItem: IsKeyboardFocusWithin trigger so focus ring shows when any child has focus",
      "TextBox: Standard focus border"
    ],
    "contract": "Every custom control either preserves a visible system focus cue or uses the AccentRingBrush custom replacement. FocusVisualStyle={x:Null} is only set when a custom ring trigger exists."
  }
}
