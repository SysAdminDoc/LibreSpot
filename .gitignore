*.bak
*.log
*.tmp
.vs/
CLAUDE.md

# .NET / WPF build outputs
bin/
obj/
publish/
publish-new/
tmp/
*.user

# Build artifacts — never tracked. Integrity comes from CI-attested release
# assets (release.yml generates fresh checksums + SBOM + provenance per tag),
# not from stale copies committed to the repo.
LibreSpot.exe
checksums.txt

# Runtime caches written to %APPDATA% in production; belt-and-braces here
update-check.json

# Markdown files local-only (except README, SECURITY, and GitHub community health)
*.md
!README.md
!SECURITY.md
!.github/*.md
!.github/**/*.md
